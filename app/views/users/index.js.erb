<% if @searched_users %>
    $('#search-results').html("<tr class='search-message'><th><%= flash.now[:notice] %></th><th></th><th></th></tr>");
    $('#search-results').append("<%= escape_javascript(render partial: 'index', locals: { users: @searched_users }) %>");
<% end %> 
